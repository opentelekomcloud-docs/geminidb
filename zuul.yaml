- project:
    merge-mode: squash-merge
    default-branch: main
    templates:
      - helpcenter-base-jobs
      - api-ref-hc-jobs
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop
